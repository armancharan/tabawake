[package]
name = "frame_engine"
version = "0.1.0"
edition = "2021"
description = "Procedural RGBA frame generator for tabawake media demo"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[dependencies]
wasm-bindgen = "0.2.100"
